#include<stdio.h>
int main()
{
int n,i,marks[5],sum=0;
printf("Enter the numbers of subject: ");
scanf("%d",&n);
for ( i = 0; i < n; i++)
{
    scanf("%d",&marks[i]);
    sum= sum + marks[i];
}
printf("sum is %d",sum);
return 0;
}
