#include<stdio.h>
void swap(int *x, int *y);
void main()
{
int a=10 ,b=20;
printf("before swapping a=%d and b=%d\n",a,b);
swap(&a,&b);
}

void swap(int *x,int *y)
{
int t;
t=*x;
*x=*y;
*y=t;
printf("After swapping a=%d, b=%d",*x,*y);
}
